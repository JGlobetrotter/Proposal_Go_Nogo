# Proposal_Go_Nogo

This is a go / no go decision for proposals
Users are NGOs


echo "# Proposal_Go_Nogo" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/JGlobetrotter/Proposal_Go_Nogo.git
git push -u origin main

# Licensing requires permission
